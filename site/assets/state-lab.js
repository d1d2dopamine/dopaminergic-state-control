(()=>{
'use strict';
const root=document.getElementById('lab-app');if(!root)return;
const $=id=>document.getElementById(id);
const clamp=(x,a,b)=>Math.max(a,Math.min(b,x));
const sigmoid=x=>1/(1+Math.exp(-x));
const median=a=>{const b=[...a].sort((x,y)=>x-y),n=b.length;return n?(n%2?b[(n-1)/2]:(b[n/2-1]+b[n/2])/2):0;};
const fmt=(v,d=3)=>Number.isFinite(Number(v))?Number(v).toFixed(d).replace(/0+$/,'').replace(/\.$/,''):'—';
let data=null,runMeta=null,committedRuns=[],baseline=null,current=null,eventCache=null,frame=0,playing=false,raf=null,lastPlayTs=0,viewer=null;

function params(){return{mode:$('lab-mode').value,candidate:$('lab-candidate').value,candidate_gain:Number($('lab-gain').value),stimulus:$('lab-stimulus').value,stimulus_strength:Number($('lab-stimulus-strength').value),pulse_start_ms:Number($('lab-pulse-start').value),pulse_duration_ms:Number($('lab-pulse-duration').value),dopamine_tone:Number($('lab-tone').value),dat_clearance:Number($('lab-dat').value),receptor_gains:{Dop1R1:Number($('lab-dop1r1').value),Dop1R2:Number($('lab-dop1r2').value),Dop2R:Number($('lab-dop2r').value)}};}
function selectedIds(p){return p.candidate==='pair'?data.candidate_ids.slice(0,2).map(Number):[Number(p.candidate)].filter(Number.isFinite);}
function copyMatrix(channels){return channels.map(ch=>ch.weights.map(Number));}
function seededShuffle(values,seed){const a=[...values];let s=(seed>>>0)||1;for(let i=a.length-1;i>0;i--){s=(1664525*s+1013904223)>>>0;const j=s%(i+1);[a[i],a[j]]=[a[j],a[i]];}return a;}
function interventionMatrix(p){const matrix=copyMatrix(data.input_channels);if(p.mode==='real')return{matrix,off:new Set()};const ids=selectedIds(p),off=new Set(),bodyIndex=new Map(data.cells.map((c,i)=>[Number(c.body_id),i])),medByChannel=matrix.map(row=>median(row));for(const id of ids){const ci=bodyIndex.get(id);if(ci===undefined)continue;if(p.mode==='knockout'){off.add(ci);continue;}if(p.mode==='medianize'){const total=matrix.reduce((s,row)=>s+row[ci],0),medTotal=medByChannel.reduce((s,v)=>s+v,0)||1;matrix.forEach((row,ri)=>row[ci]=medByChannel[ri]/medTotal*total);}else if(p.mode==='amplify'){let best=0;for(let ri=1;ri<matrix.length;ri++)if(matrix[ri][ci]>matrix[best][ci])best=ri;matrix[best][ci]*=p.candidate_gain;}else if(p.mode==='shuffle'){const values=matrix.map(row=>row[ci]),shuffled=seededShuffle(values,id);matrix.forEach((row,ri)=>row[ci]=shuffled[ri]);}}return{matrix,off};}

function simulate(p,forceReal=false){const model=data.model,dt=Number(model.dt_ms||20),duration=Number(model.duration_ms||3000),steps=Math.floor(duration/dt)+1,mode=forceReal?'real':p.mode,effective={...p,mode},mod=interventionMatrix(effective),W=mod.matrix,outW=copyMatrix(data.output_channels),n=data.cells.length,inputTotals=Array.from({length:n},(_,i)=>W.reduce((s,row)=>s+row[i],0)),outputTotals=Array.from({length:n},(_,i)=>outW.reduce((s,row)=>s+row[i],0)),medIn=median(inputTotals.filter(x=>x>0))||1,medOut=median(outputTotals.filter(x=>x>0))||1,stimIndex=Math.max(0,data.input_channels.findIndex(ch=>ch.name===p.stimulus)),a=new Array(n).fill(0),traces={time:[],dopamine:[],Dop1R1:[],Dop1R2:[],Dop2R:[],coupling:[],cells:[],outputs:[]};let dopamine=0;const slope=Number(model.activation_slope||8),threshold=Number(model.activation_threshold||.34),tau=Number(model.cell_tau_ms||120),tauD=Number(model.dopamine_tau_ms||380),base=Number(model.baseline_input||.08),rec=model.receptors||{};for(let step=0;step<steps;step++){const t=step*dt,pulse=t>=p.pulse_start_ms&&t<(p.pulse_start_ms+p.pulse_duration_ms);for(let i=0;i<n;i++){const total=inputTotals[i]||1,share=W[stimIndex]?.[i]/total||0,capacity=clamp(Math.sqrt(total/medIn),.45,1.8),drive=base+capacity*share*(pulse?p.stimulus_strength:0),target=mod.off.has(i)?0:sigmoid(slope*(drive-threshold));a[i]+=clamp(dt/tau,0,1)*(target-a[i]);}const release=a.reduce((s,v,i)=>s+v*clamp(Math.sqrt((outputTotals[i]||1)/medOut),.45,1.8),0)/Math.max(1,n);dopamine+=clamp(dt/tauD,0,1)*((p.dopamine_tone+release)-p.dat_clearance*dopamine);dopamine=Math.max(0,dopamine);const receptor={};for(const name of ['Dop1R1','Dop1R2','Dop2R']){const r=rec[name]||{},half=Number(r.half_activation||.4),gain=Number(p.receptor_gains[name]??r.gain??1);receptor[name]=gain*dopamine/(half+dopamine+1e-9);}const coupling=['Dop1R1','Dop1R2','Dop2R'].reduce((sum,name)=>sum+receptor[name]*Number(rec[name]?.coupling_prior||0),0),outputs=outW.map(row=>{let num=0,den=0;for(let i=0;i<n;i++){num+=a[i]*row[i];den+=row[i];}return den?num/den:0;});traces.time.push(t);traces.dopamine.push(dopamine);traces.Dop1R1.push(receptor.Dop1R1);traces.Dop1R2.push(receptor.Dop1R2);traces.Dop2R.push(receptor.Dop2R);traces.coupling.push(coupling);traces.cells.push([...a]);traces.outputs.push(outputs);}traces.params=effective;return traces;}
function peak(arr){return arr.length?Math.max(...arr):0;}
function auc(arr,dt){return arr.reduce((s,v)=>s+Number(v||0)*dt,0);}

function rateEvents(series,id,maxRate){const dt=Number(data.model.dt_ms||20),out=[];let phase=((Number(id)||1)%17)/17;for(let i=0;i<series.length;i++){phase+=Math.max(0,Number(series[i]||0))*maxRate*dt/1000;while(phase>=1){out.push(current.time[i]);phase-=1;}}return out;}
function buildEvents(){const p=params(),maxRate=Number(data.model.visual_event_rate_hz||28),cells={};data.cells.forEach((c,i)=>{cells[String(c.body_id)]=rateEvents(current.cells.map(row=>row[i]),c.body_id,maxRate);});const interval=Math.max(28,1000/(12+Math.max(0,p.stimulus_strength)*12)),upstream=[];for(let t=p.pulse_start_ms;t<p.pulse_start_ms+p.pulse_duration_ms;t+=interval)upstream.push(t);const outputs={};data.output_channels.forEach((ch,i)=>{outputs[ch.name]=rateEvents(current.outputs.map(row=>row[i]),i+911,maxRate*.72);});eventCache={upstream,cells,outputs};}
function topOutputChannels(n=4){const rows=data.output_channels.map((ch,i)=>({name:ch.name,i,pk:peak(current.outputs.map(r=>r[i]))})).filter(x=>x.name!=='other').sort((a,b)=>b.pk-a.pk);return rows.slice(0,n);}

function run(){const p=params();baseline=simulate(p,true);current=simulate(p,false);buildEvents();frame=0;$('lab-time').value='0';viewer?.setSelection(p.candidate,p.stimulus,{fit:!viewerSelectionReady});viewerSelectionReady=true;renderAll();}
let viewerSelectionReady=false;
function setLabels(){$('lab-gain-value').textContent=`${Number($('lab-gain').value).toFixed(1)}×`;$('lab-stimulus-value').textContent=Number($('lab-stimulus-strength').value).toFixed(2);$('lab-pulse-start-value').textContent=`${$('lab-pulse-start').value} ms`;$('lab-pulse-duration-value').textContent=`${$('lab-pulse-duration').value} ms`;$('lab-tone-value').textContent=Number($('lab-tone').value).toFixed(2);$('lab-dat-value').textContent=`${Number($('lab-dat').value).toFixed(2)}×`;for(const id of ['dop1r1','dop1r2','dop2r'])$(`lab-${id}-value`).textContent=`${Number($(`lab-${id}`).value).toFixed(2)}×`;if($('lab-brain-opacity-value'))$('lab-brain-opacity-value').textContent=Number($('lab-brain-opacity').value).toFixed(2);}
function summary(){const p=params(),ids=selectedIds(p),maxDA=peak(current.dopamine),baseDA=peak(baseline.dopamine),delta=maxDA-baseDA,cells=ids.map(id=>data.cells.findIndex(c=>Number(c.body_id)===id)).filter(i=>i>=0),maxA=cells.length?Math.max(...cells.map(i=>Math.max(...current.cells.map(row=>row[i])))):0,events=cells.reduce((s,i)=>s+(eventCache.cells[String(data.cells[i].body_id)]||[]).length,0);$('lab-summary').innerHTML=`<div><dt>scenario</dt><dd>${p.mode}</dd></div><div><dt>candidate</dt><dd>${p.candidate}</dd></div><div><dt>candidate peak</dt><dd>${fmt(maxA)}</dd></div><div><dt>sim events</dt><dd>${events}</dd></div><div><dt>peak DA</dt><dd>${fmt(maxDA)} <span class="muted">Δ ${delta>=0?'+':''}${fmt(delta)}</span></dd></div>`;}

function renderCells(){const cur=current.cells[frame],base=baseline.cells[frame],cand=new Set(selectedIds(params()));$('lab-cell-grid').innerHTML=data.cells.map((c,i)=>`<div class="cell-row ${cand.has(Number(c.body_id))?'candidate':''}"><a href="network.html?focus=${c.body_id}" title="open in 3D specimen">${c.body_id}</a><span class="cell-side">${c.side||'—'}</span><div class="cell-bar"><i style="width:${clamp(base[i]*100,0,100)}%"></i><b style="width:${clamp(cur[i]*100,0,100)}%"></b></div><span>${fmt(cur[i],2)}</span></div>`).join('');}
function renderDossier(){const p=params(),id=selectedIds(p)[0],c=data.cells.find(x=>Number(x.body_id)===id)||data.cells[0];if(!c)return;const m=c.metrics||{},rob=c.robustness;let html=`<dl class="kv lab-kv"><dt>body</dt><dd>${c.body_id} (${c.side})</dd><dt>max input share</dt><dd>${fmt(m.max_input_share)} · z ${fmt(m.max_input_share_z)}</dd><dt>input strength</dt><dd>${fmt(m.in_strength,0)}</dd><dt>input partners</dt><dd>${fmt(m.in_partner_count,0)}</dd><dt>discovery</dt><dd>${c.candidate_status||'manual candidate'}</dd><dt>thresholds</dt><dd>${rob?`${rob.passed_thresholds}/${rob.available_thresholds}`:'—'}</dd></dl>`;html+=`<div class="lab-subhead">strongest inputs</div><table class="compact"><thead><tr><th>type</th><th>body</th><th>w</th><th>share</th></tr></thead><tbody>${c.top_inputs.slice(0,8).map(x=>`<tr><td>${x.type}</td><td>${x.body_id}</td><td>${x.weight}</td><td>${(x.share*100).toFixed(1)}%</td></tr>`).join('')}</tbody></table>`;html+=`<div class="lab-subhead">strongest outputs</div><table class="compact"><thead><tr><th>type</th><th>body</th><th>w</th><th>share</th></tr></thead><tbody>${c.top_outputs.slice(0,8).map(x=>`<tr><td>${x.type}</td><td>${x.body_id}</td><td>${x.weight}</td><td>${(x.share*100).toFixed(1)}%</td></tr>`).join('')}</tbody></table>`;$('lab-candidate-dossier').innerHTML=html;}
function renderOutputs(){const cur=current.outputs[frame],base=baseline.outputs[frame],rows=data.output_channels.map((ch,i)=>({name:ch.name,cur:cur[i],base:base[i],delta:cur[i]-base[i]})).sort((a,b)=>b.cur-a.cur).slice(0,10);$('lab-output-table').innerHTML=`<table class="compact"><thead><tr><th>target type</th><th>real</th><th>current</th><th>Δ</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${r.name}</td><td>${fmt(r.base,2)}</td><td>${fmt(r.cur,2)}</td><td>${r.delta>=0?'+':''}${fmt(r.delta,2)}</td></tr>`).join('')}</tbody></table>`;}
function renderDistribution(){const max=Math.max(...data.distribution.cells.map(x=>x.value),.001),cands=new Set(data.candidate_ids.map(Number));$('lab-distribution').innerHTML=data.distribution.cells.map(x=>`<div class="dist-row ${cands.has(Number(x.body_id))?'candidate':''}"><span>${x.body_id}</span><span>${x.side}</span><div><i style="width:${x.value/max*100}%"></i></div><span>${(x.value*100).toFixed(1)}%</span><span>z ${fmt(x.robust_z,2)}</span></div>`).join('');}

function prepCanvas(canvas,height){const d=Math.min(devicePixelRatio||1,1.6),w=Math.max(320,Math.floor(canvas.clientWidth*d)),h=Math.max(140,Math.floor(height*d));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;}return{ctx:canvas.getContext('2d'),d,w,h};}
function drawRaster(){const canvas=$('lab-raster-canvas'),{ctx,d,w,h}=prepCanvas(canvas,250),p=params(),ids=selectedIds(p),outs=topOutputChannels(4),rows=[{label:p.stimulus,events:eventCache.upstream,strong:true},...ids.map(id=>({label:`PAM04 ${id}`,events:eventCache.cells[String(id)]||[],strong:true})),...outs.map(o=>({label:o.name,events:eventCache.outputs[o.name]||[]}))],left=104*d,right=14*d,top=22*d,bottom=24*d,ww=w-left-right,hh=h-top-bottom,rowH=hh/Math.max(1,rows.length),duration=current.time.at(-1)||1;ctx.clearRect(0,0,w,h);ctx.fillStyle='#0c0d0f';ctx.fillRect(0,0,w,h);ctx.font=`${10*d}px ui-monospace,monospace`;rows.forEach((r,ri)=>{const y=top+rowH*(ri+.5);ctx.fillStyle=r.strong?'#ddd':'#888';ctx.fillText(r.label,6*d,y+3*d);ctx.strokeStyle='#25272b';ctx.beginPath();ctx.moveTo(left,y);ctx.lineTo(w-right,y);ctx.stroke();ctx.strokeStyle=r.strong?'#f2f2f2':'#a4a4a4';ctx.lineWidth=r.strong?1.5*d:1*d;for(const t of r.events){const x=left+ww*t/duration;ctx.beginPath();ctx.moveTo(x,y-rowH*.28);ctx.lineTo(x,y+rowH*.28);ctx.stroke();}});const x=left+ww*(current.time[frame]/duration);ctx.strokeStyle='#fff';ctx.globalAlpha=.65;ctx.beginPath();ctx.moveTo(x,top);ctx.lineTo(x,h-bottom);ctx.stroke();ctx.globalAlpha=1;ctx.fillStyle='#777';ctx.fillText('SIMULATED EVENTS',left,12*d);ctx.fillText(`${Math.round(current.time[frame])} ms`,w-78*d,h-6*d);}
function drawActivity(){const canvas=$('lab-activity-canvas'),{ctx,d,w,h}=prepCanvas(canvas,235),p=params(),ids=selectedIds(p),id=ids[0],ci=data.cells.findIndex(c=>Number(c.body_id)===id),left=42*d,right=15*d,top=20*d,bottom=27*d,ww=w-left-right,hh=h-top-bottom,duration=current.time.at(-1)||1;ctx.clearRect(0,0,w,h);ctx.fillStyle='#0c0d0f';ctx.fillRect(0,0,w,h);ctx.strokeStyle='#25272b';for(let k=0;k<=4;k++){const y=top+hh*k/4;ctx.beginPath();ctx.moveTo(left,y);ctx.lineTo(w-right,y);ctx.stroke();}const line=(arr,alpha,dash)=>{ctx.globalAlpha=alpha;ctx.strokeStyle='#f0f0f0';ctx.lineWidth=(dash?1:1.7)*d;ctx.setLineDash(dash?[5*d,4*d]:[]);ctx.beginPath();arr.forEach((v,i)=>{const x=left+ww*i/(arr.length-1),y=top+hh*(1-clamp(v,0,1));i?ctx.lineTo(x,y):ctx.moveTo(x,y);});ctx.stroke();};if(ci>=0){line(baseline.cells.map(r=>r[ci]),.35,true);line(current.cells.map(r=>r[ci]),1,false);}ctx.setLineDash([]);const maxDA=Math.max(.001,peak(current.dopamine));ctx.strokeStyle='#777';ctx.lineWidth=1*d;ctx.beginPath();current.dopamine.forEach((v,i)=>{const x=left+ww*i/(current.dopamine.length-1),y=top+hh*(1-clamp(v/maxDA,0,1));i?ctx.lineTo(x,y):ctx.moveTo(x,y);});ctx.stroke();const x=left+ww*(current.time[frame]/duration);ctx.strokeStyle='#fff';ctx.globalAlpha=.55;ctx.beginPath();ctx.moveTo(x,top);ctx.lineTo(x,h-bottom);ctx.stroke();ctx.globalAlpha=1;ctx.fillStyle='#aaa';ctx.font=`${10*d}px ui-monospace,monospace`;ctx.fillText(`PAM04 ${id} activity`,left,12*d);ctx.fillStyle='#777';ctx.fillText('DA scaled',w-75*d,12*d);}
function drawCircuit(){const canvas=$('lab-circuit-canvas'),{ctx,d,w,h}=prepCanvas(canvas,250),p=params(),ids=selectedIds(p),outs=topOutputChannels(3),candidateActivity=Math.max(...ids.map(id=>{const i=data.cells.findIndex(c=>Number(c.body_id)===id);return i>=0?current.cells[frame][i]:0;}),0),up=p.pulse_start_ms<=current.time[frame]&&current.time[frame]<p.pulse_start_ms+p.pulse_duration_ms,left=[w*.18,h*.5],mid=[w*.5,h*.5],rightX=w*.82;ctx.clearRect(0,0,w,h);ctx.fillStyle='#0c0d0f';ctx.fillRect(0,0,w,h);const node=(x,y,label,a)=>{ctx.beginPath();ctx.arc(x,y,(8+8*clamp(a,0,1))*d,0,Math.PI*2);ctx.fillStyle=`rgba(240,240,240,${.12+.8*clamp(a,0,1)})`;ctx.fill();ctx.fillStyle=a>.5?'#eee':'#888';ctx.font=`${10*d}px ui-monospace,monospace`;ctx.textAlign='center';ctx.fillText(label,x,y+27*d);};const edge=(a,b,v)=>{ctx.strokeStyle=`rgba(220,220,220,${.12+.7*clamp(v,0,1)})`;ctx.lineWidth=(1+3*clamp(v,0,1))*d;ctx.beginPath();ctx.moveTo(a[0],a[1]);ctx.lineTo(b[0],b[1]);ctx.stroke();};const outPts=outs.map((o,i)=>[rightX,h*(.24+i*.26)]);edge(left,mid,up?1:.05);outPts.forEach((pt,i)=>edge(mid,pt,current.outputs[frame][outs[i].i]||0));node(left[0],left[1],p.stimulus,up?1:.05);node(mid[0],mid[1],p.candidate==='pair'?'PAM04 pair':`PAM04 ${p.candidate}`,candidateActivity);outPts.forEach((pt,i)=>node(pt[0],pt[1],outs[i].name,current.outputs[frame][outs[i].i]||0));ctx.textAlign='left';ctx.fillStyle='#666';ctx.fillText('structural path / simulated state',8*d,13*d);}

function renderFrame(){if(!current||!baseline)return;const t=current.time[frame],p=params(),pulse=t>=p.pulse_start_ms&&t<(p.pulse_start_ms+p.pulse_duration_ms);$('lab-time-value').textContent=`${Math.round(t)} ms`;$('lab-time').value=String(Math.round(frame/(current.time.length-1)*100));renderCells();renderOutputs();drawRaster();drawActivity();drawCircuit();const r={Dop1R1:current.Dop1R1[frame],Dop1R2:current.Dop1R2[frame],Dop2R:current.Dop2R[frame]},da=current.dopamine[frame],ci=current.coupling[frame];$('lab-cell-note').textContent=`t ${Math.round(t)} ms · DA ${fmt(da)} · coupling ${fmt(ci)}`;$('lab-receptor-readout').innerHTML=`<span>DA <b>${fmt(da)}</b></span><span>Dop1R1 <b>${fmt(r.Dop1R1)}</b></span><span>Dop1R2 <b>${fmt(r.Dop1R2)}</b></span><span>Dop2R <b>${fmt(r.Dop2R)}</b></span>`;viewer?.updateFrame({timeMs:t,cellActivity:current.cells[frame],outputs:current.outputs[frame],upstreamActive:pulse,events:eventCache});}
function renderAll(){setLabels();summary();renderDossier();renderDistribution();renderFrame();}

function stop(){playing=false;if(raf){cancelAnimationFrame(raf);raf=null;}$('lab-play').textContent='▶ play';lastPlayTs=0;}
function tick(ts){if(!playing)return;if(!lastPlayTs)lastPlayTs=ts;const speed=Number($('lab-speed').value||1),dt=Number(data.model.dt_ms||20),frames=Math.max(1,Math.floor((ts-lastPlayTs)*speed/dt));if(frames>0){frame+=frames;lastPlayTs+=frames*dt/speed;if(frame>=current.time.length){if($('lab-loop').checked){frame=0;lastPlayTs=ts;}else{frame=current.time.length-1;renderFrame();stop();return;}}renderFrame();}raf=requestAnimationFrame(tick);}
function play(){if(!current)run();if(playing){stop();return;}playing=true;$('lab-play').textContent='❚❚ pause';lastPlayTs=0;raf=requestAnimationFrame(tick);}
function reset(){stop();frame=0;renderFrame();}
function step(delta){stop();frame=clamp(frame+delta,0,current.time.length-1);renderFrame();}

function copyState(){const p=params(),u=new URL(location.href);Object.entries({mode:p.mode,candidate:p.candidate,input:p.stimulus,strength:p.stimulus_strength,start:p.pulse_start_ms,duration:p.pulse_duration_ms,tone:p.dopamine_tone,dat:p.dat_clearance,r1:p.receptor_gains.Dop1R1,r2:p.receptor_gains.Dop1R2,d2:p.receptor_gains.Dop2R,gain:p.candidate_gain}).forEach(([k,v])=>u.searchParams.set(k,String(v)));navigator.clipboard?.writeText(u.toString());$('lab-copy-url').textContent='copied';setTimeout(()=>$('lab-copy-url').textContent='copy state URL',1000);}
function exportRun(){const p=params(),ids=selectedIds(p),dt=Number(data.model.dt_ms||20),cellIndices=ids.map(id=>data.cells.findIndex(c=>Number(c.body_id)===id)).filter(i=>i>=0),summaryPayload={baseline_peak_dopamine:peak(baseline.dopamine),intervention_peak_dopamine:peak(current.dopamine),delta_peak_dopamine:peak(current.dopamine)-peak(baseline.dopamine),candidate_peak_activity:Math.max(0,...cellIndices.map(i=>peak(current.cells.map(r=>r[i])))),candidate_activity_auc:cellIndices.reduce((s,i)=>s+auc(current.cells.map(r=>r[i]),dt),0),candidate_simulated_events:cellIndices.reduce((s,i)=>s+(eventCache.cells[String(data.cells[i].body_id)]||[]).length,0),dopamine_auc:auc(current.dopamine,dt)};const payload={experiment:data.experiment_id,dataset:data.dataset,exported_at:new Date().toISOString(),source_run:runMeta?{commit:runMeta.git_sha||runMeta.git?.commit||runMeta.git_commit||null,dataset:runMeta.dataset||null,counts:runMeta.counts||null,effective_analysis:runMeta.effective_analysis||null}:null,parameters:p,model:data.model,assumptions:data.assumptions,visualisation:{event_raster:'deterministic events derived from normalized rate activity; not recorded spikes',pulse_fronts:'animation over real skeleton geometry; travel time is a visual model parameter'},summary:summaryPayload};const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`pam04-${p.mode}-${Date.now()}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500);}
function renderCommittedRuns(){const host=$('lab-committed-runs');if(!host)return;if(!committedRuns.length){host.textContent='No committed State Lab scenarios were reproduced in this build.';return;}host.innerHTML=`<table class="compact"><thead><tr><th>run</th><th>mode</th><th>candidate</th><th>stimulus</th><th>Δ peak DA</th></tr></thead><tbody>${committedRuns.map(r=>`<tr><td>${r.name}</td><td>${r.parameters?.mode||'—'}</td><td>${r.parameters?.candidate||'—'}</td><td>${r.parameters?.stimulus||'—'}</td><td>${fmt(r.summary?.delta_peak_dopamine||0,4)}</td></tr>`).join('')}</tbody></table>`;}
function loadURL(){const q=new URLSearchParams(location.search),set=(id,key)=>{if(q.has(key))$(id).value=q.get(key);};set('lab-mode','mode');set('lab-candidate','candidate');set('lab-stimulus','input');set('lab-stimulus-strength','strength');set('lab-pulse-start','start');set('lab-pulse-duration','duration');set('lab-tone','tone');set('lab-dat','dat');set('lab-dop1r1','r1');set('lab-dop1r2','r2');set('lab-dop2r','d2');set('lab-gain','gain');}

async function init(){const [r,runResponse,runsResponse]=await Promise.all([fetch('data/experiments/001_pam04.json'),fetch('data/run.json').catch(()=>null),fetch('data/experiments/runs/index.json').catch(()=>null)]);if(!r.ok)throw new Error(`experiment data: ${r.status}`);data=await r.json();if(runResponse?.ok)runMeta=await runResponse.json();if(runsResponse?.ok)committedRuns=await runsResponse.json();if(!data.available){$('lab-unavailable').hidden=false;$('lab-unavailable').textContent=data.reason||'PAM04 experiment unavailable in this snapshot.';return;}root.hidden=false;const candidates=data.candidate_ids.length?data.candidate_ids:data.cells.slice(0,2).map(c=>c.body_id);$('lab-candidate').innerHTML=candidates.map(id=>`<option value="${id}">${id}</option>`).join('')+(candidates.length>1?'<option value="pair">bilateral candidate pair</option>':'');$('lab-stimulus').innerHTML=data.input_channels.filter(ch=>ch.name!=='other').map(ch=>`<option value="${ch.name}">${ch.name} · w=${Math.round(ch.total_weight)}</option>`).join('');const first=data.cells.find(c=>Number(c.body_id)===Number(candidates[0]));if(first?.top_inputs?.length){const preferred=first.top_inputs[0].type;if(data.input_channels.some(ch=>ch.name===preferred))$('lab-stimulus').value=preferred;}$('lab-assumptions').innerHTML=data.assumptions.map(x=>`<li>${x}</li>`).join('');renderCommittedRuns();loadURL();viewer=window.DSCStateLab3D?.create($('lab-3d-canvas'),data,{statusEl:$('lab-3d-status')})||null;
  const rerunIds=new Set(['lab-mode','lab-gain','lab-stimulus','lab-stimulus-strength','lab-pulse-start','lab-pulse-duration','lab-tone','lab-dat','lab-dop1r1','lab-dop1r2','lab-dop2r']);let rerunTimer=null;const scheduleRun=()=>{clearTimeout(rerunTimer);rerunTimer=setTimeout(()=>{stop();run();},70);};document.querySelectorAll('.lab-controls input,.lab-controls select').forEach(el=>el.addEventListener('input',()=>{setLabels();if(el.id==='lab-candidate'){if(el.value!=='pair'){const cell=data.cells.find(c=>String(c.body_id)===el.value),preferred=cell?.top_inputs?.[0]?.type;if(preferred&&data.input_channels.some(ch=>ch.name===preferred))$('lab-stimulus').value=preferred;}renderDossier();viewer?.setSelection($('lab-candidate').value,$('lab-stimulus').value,{fit:true});scheduleRun();}else if(el.id==='lab-stimulus'){viewer?.setSelection($('lab-candidate').value,$('lab-stimulus').value,{fit:false});scheduleRun();}else if(rerunIds.has(el.id))scheduleRun();}));
  $('lab-run').onclick=()=>{stop();run();};$('lab-play').onclick=play;$('lab-reset').onclick=reset;$('lab-prev').onclick=()=>step(-1);$('lab-next').onclick=()=>step(1);$('lab-copy-url').onclick=copyState;$('lab-export').onclick=exportRun;$('lab-time').oninput=()=>{stop();frame=Math.round(Number($('lab-time').value)/100*(current.time.length-1));renderFrame();};$('lab-follow').oninput=()=>viewer?.setFollow($('lab-follow').checked);$('lab-brain-opacity').oninput=()=>{setLabels();viewer?.setBrainOpacity(Number($('lab-brain-opacity').value));};$('lab-fit-candidate').onclick=()=>viewer?.fitCandidate();$('lab-fit-brain').onclick=()=>viewer?.fitBrain();$('lab-view-anterior').onclick=()=>viewer?.setPreset('anterior');$('lab-view-dorsal').onclick=()=>viewer?.setPreset('dorsal');window.addEventListener('resize',()=>{drawRaster();drawActivity();drawCircuit();viewer?.requestRender();});run();}
init().catch(err=>{$('lab-unavailable').hidden=false;$('lab-unavailable').textContent=`State Lab failed: ${err.message}`;console.error(err);});
})();
